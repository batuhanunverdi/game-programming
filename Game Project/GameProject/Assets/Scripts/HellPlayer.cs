using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HellPlayer : MonoBehaviour
{
    public static List<GameObject> playerListHell;
    // Start is called before the first frame update
    void Start()
    {
        playerListHell = new List<GameObject>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
